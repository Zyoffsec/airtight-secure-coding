# Airtight — Gate Checklist

> One line per gate: the rule and its fix. Run this list before emitting code.

- Gate 1 — Auth response uniformity. Fix: hash unconditionally. Compute one throwaway hash at startup with the same parameters as real ones, verify against it when the lookup misses, and collapse both outcomes into one 401 with one body. For reset requests, always answer "if that address has an account, a link has been sent" — after the same work either way. (OWASP A07, CWE-287)
- Gate 2 — Constant-time secret comparison. Fix: compare with `crypto.timingSafeEqual` on equal-length buffers (Python: `hmac.compare_digest`). For passwords, do not compare at all — call the hashing library's verify, which is constant-time by construction (Gate 3). (OWASP A07, CWE-287)
- Gate 3 — Password storage. Fix: hash with argon2id (`argon2-cffi`), or bcrypt where argon2 is unavailable. Store the library's full encoded output — it carries the salt and parameters — and verify with the library's own verify function, never by re-hashing and comparing strings. (OWASP A07, CWE-287)
- Gate 5 — Session lifecycle. Fix: call the session store's regenerate on successful login and its destroy on logout, instead of assigning and clearing a `userId` field. Apply the same destroy to every session the user owns when their password changes. (OWASP A07, CWE-287)
- Gate 6 — Identity token entropy. Fix: use `crypto.randomBytes(32).toString("base64url")` (Python: `secrets.token_urlsafe(32)`). Where session middleware mints its own identifier, let it. (OWASP A07, CWE-287)
- Gate 7 — Session cookie flags. Fix: set all three. Each guards a distinct takeover path — `httpOnly` keeps injected script from reading the cookie, `secure` keeps it off plain HTTP, `sameSite` keeps other sites from sending it. If local dev runs without TLS, gate only `secure` on the environment — never the other two. (OWASP A07, CWE-287)
- Gate 8 — Reset token expiry and single use. Fix: give the token row an `expiresAt` (an hour is generous) and a `usedAt`; reject when either check fails; write `usedAt` in the same transaction as the password update so two concurrent redemptions cannot both land. (OWASP A07, CWE-287)
- Gate 10 — Server-side identity. Fix: Take identity only from `req.session` (or verified token claims), never from the request. When the client must name a different user (e.g. an admin), that is privileged and Gate 11 applies. (OWASP A01, CWE-284)
- Gate 11 — Privileged route authorization. Fix: Add a role check to the handler chain that reads the role from the session or a server-side (OWASP A01, CWE-284)
- Gate 12 — Object ownership check. Fix: Put the ownership column in the `WHERE` clause of the same query that fetches the record, so (OWASP A01, CWE-284)
- Gate 13 — Mass assignment allowlist. Fix: Declare a schema listing only the editable fields (a Pydantic model, a zod schema, a (OWASP A01, CWE-284)
- Gate 15 — Tenant scoping. Fix: Resolve the tenant to the session user's membership before querying, and pass the resolved (OWASP A01, CWE-284)
- Gate 21 — Dynamic SQL identifiers. Fix: Look the value up in an object of literal strings written in the source, falling back to a (OWASP A03, CWE-74)
- Gate 22 — SQL query construction. Fix: Pass values as bound parameters using the driver's placeholder syntax and let the driver do (OWASP A03, CWE-74)
- Gate 24 — Query operator injection. Fix: Coerce at the boundary: `String(...)` for strings, `Number(...)` for numbers, and reject if the result is not what you expected. (OWASP A03, CWE-74)
- Gate 26 — OS command construction. Fix: Pass the command as an argument list with no shell (`shell=False` default; in Node, `execFile`/`spawn` without `shell: true`). The kernel receives the argument vector directly, so there is no shell to interpret `;`, `$()` or backticks — escaping helpers like `shlex.quote` are not the remedy. Whether `filename` is a plausible filename is input validation (Gates 40-49). (OWASP A03, CWE-74)
- Gate 28 — Runtime evaluation of user input. Fix: Keep the template source literal and pass the value as a named argument, so the engine parses (OWASP A03, CWE-74)
- Gate 30 — Hardcoded credential literal. Fix: Read the value from the environment at the point of use (`process.env.STRIPE_SECRET_KEY`) and let the process fail if absent (Gate 31). If it was already in source, it is leaked: Gate 33 applies. (OWASP A05, CWE-798)
- Gate 31 — Secret fallback default. Fix: Index the environment directly (`os.environ["SECRET_KEY"]`, or a schema that marks the field required) so a missing value stops the process at startup. (OWASP A05, CWE-798)
- Gate 32 — Ignore rule before the file. Fix: Write the `.gitignore` rule in the same change, before the file, with `!.env.example` so the template stays tracked. If the file is already tracked, Gate 33 applies. (OWASP A05, CWE-798)
- Gate 33 — Removing a committed secret. Fix: Move the value to `os.environ[...]` **and** state that the key is compromised, naming the revocation step at the issuer. The key is in history and still live; deleting the line does not un-publish it, and rewriting history does not substitute for revocation. (OWASP A05, CWE-798)
- Gate 35 — Secrets in the client bundle. Fix: Put the call behind a server route that holds the key server-side and forward the result. The browser gets the answer, never the credential. (OWASP A05, CWE-798)
- Gate 37 — Secrets in error responses. Fix: Return a literal message and status code (`res.status(500).json({ error: "Internal server error" })`). Send the exception to the log instead. (OWASP A05, CWE-798)
- Gate 40 — Server-side request validation. Fix: Parse at the top of the handler with a runtime schema (zod, Pydantic) and use only its (OWASP A03, CWE-20)
- Gate 41 — Server-computed amounts. Fix: Accept only identifiers and quantities from the client, look up every price server-side by (OWASP A03, CWE-20)
- Gate 43 — Validated value is the one used. Fix: Use only the validator's output for the rest of the handler — `serializer.validated_data`, (OWASP A03, CWE-20)
- Gate 44 — Upload type from content. Fix: Sniff the bytes server-side (`file-type` in Node, `python-magic` or a Pillow (OWASP A03, CWE-20)
- Gate 45 — Upload size cap. Fix: Set `limits.fileSize` and `limits.files` on the upload instance to the largest file the (OWASP A03, CWE-20)
- Gate 46 — Upload destination path. Fix: Generate the filename server-side — a UUID plus the extension from the Gate 44 sniff — write (OWASP A03, CWE-20)
- Gate 50 — Supplied HTML at a raw sink. Fix: sanitize at the sink, as the last expression before the value enters it. (OWASP A03, CWE-79)
- Gate 51 — Markup assembled by interpolation. Fix: do not build the HTML. Set `textContent`, return a JSX child (`<li>{item.title}</li>`), or use a template placeholder; in Python, `format_html("<b>{}</b>", user.name)` escapes the argument. (OWASP A03, CWE-79)
- Gate 53 — URL scheme allowlist. Fix: parse with `new URL(raw)` and test `url.protocol` against a literal `Set`, then use `url.href`. (OWASP A03, CWE-79)
- Gate 55 — Data in a script context. Fix: serialize with a helper that escapes `<` — `serialize-javascript` (Node), (OWASP A03, CWE-79)
- Gate 60 — TLS verification. Fix: Supply the peer's CA as an explicit trust anchor so the one certificate you meant to accept (OWASP A02, CWE-327)
- Gate 61 — Unpredictable values from a CSPRNG. Fix: Draw from the CSPRNG: `crypto.randomInt(n)` per character where the value has a format to (OWASP A02, CWE-327)
- Gate 63 — Cipher mode and nonce. Fix: Use `aes-256-gcm` with a 12-byte nonce from `crypto.randomBytes` generated *inside* the (OWASP A02, CWE-327)
- Gate 65 — Encoding is not protection. Fix: Attach an HMAC-SHA256 of the value computed with a server-side key and verify it with (OWASP A02, CWE-327)
- Gate 70 — Outbound destination allowlist. Fix: Parse the URL, check `protocol` and `hostname` against a `Set` of literal values written in (OWASP A10, CWE-918)
- Gate 71 — Redirects on a fetched URL. Fix: Set `redirect: "manual"` (undici `maxRedirections: 0`, Python `allow_redirects=False`) and (OWASP A10, CWE-918)
- Gate 74 — Stored callback destination. Fix: Send webhook traffic through a dedicated egress proxy that allows only public destinations, (OWASP A10, CWE-918)
- Gate 76 — Remote resources in rendered documents. Fix: Turn on request interception and abort by default, continuing only for a scheme and host on (OWASP A10, CWE-918)
- Gate 80 — CORS origin allowlist. Fix: Enumerate the origins as literal strings in the source and let the middleware match the (OWASP A05, CWE-16)
- Gate 81 — Debug mode in a deployed configuration. Fix: Start the app from a production server in the deployed configuration (`gunicorn`, (OWASP A05, CWE-16)
- Gate 83 — Default account credential. Fix: Read the value from the environment with no fallback and let the seed refuse to run without (OWASP A05, CWE-16)
- Gate 85 — Datastore and admin surface exposure. Fix: Drop the `ports` entry for anything only other services talk to, and prefix the rest with (OWASP A05, CWE-16)
- Gate 87 — Permissions on generated secret files. Fix: Pass the mode at creation — `{ mode: 0o600 }` in Node, `os.open(path, os.O_WRONLY | (OWASP A05, CWE-16)
- Gate 90 — Untrusted deserialization. Fix: Parse into a declared schema with a data-only format — `Workspace.model_validate_json` (OWASP A08, CWE-345)
- Gate 91 — Third-party script integrity. Fix: Pin an exact version in the URL, add `integrity` with the SHA-384 digest of that exact (OWASP A08, CWE-345)
- Gate 93 — Code fetched then executed. Fix: Download to a file, verify with `sha256sum -c` against a digest written in the source, then (OWASP A08, CWE-345)
- Gate 95 — Update and plugin signature. Fix: Sign the release artifact in the release process with an ed25519 key, ship the public half (OWASP A08, CWE-345)
- Gate 96 — Inbound webhook signature. Fix: Verify with the vendor's own constructor as the handler's first statement (OWASP A08, CWE-345)
- Gate 100 — Named fields in log records. Fix: Build an object literal from the fields the record needs — `{ event: "auth.login", email, (OWASP A09, CWE-778)
- Gate 101 — Swallowed exception. Fix: Give every catch a body that logs the caught error with enough context to identify the path (OWASP A09, CWE-778)
- Gate 103 — Security events recorded. Fix: Emit one structured record per security-relevant outcome, carrying a stable `event` name, (OWASP A09, CWE-778)
- Gate 107 — Log destination is collected. Fix: Log to stdout unconditionally and let the platform's collector own everything downstream — (OWASP A09, CWE-778)
- Gate 110 — Lockfile-honouring install. Fix: Commit the lockfile, copy it in ahead of the install so the layer cache still works (OWASP A06, CWE-1104)
- Gate 112 — Floating version specifier. Fix: Pin every direct dependency to a version, and compile the transitive set with `pip-compile` (OWASP A06, CWE-1104)
- Gate 114 — Non-registry source pinning. Fix: Pin the full 40-character commit SHA (content-addressed: you get the tree you reviewed, or (OWASP A06, CWE-1104)
- Gate 115 — Package name provenance. Fix: Emit only names you can point at a source for — the manifest, the lockfile, or the (OWASP A06, CWE-1104)
- Gate 117 — Audit step in the pipeline. Fix: Add the audit as its own step after the install, in the workflow that already runs the (OWASP A06, CWE-1104)
- Gate 118 — Suppressed audit result. Fix: Upgrade to the fixed release. Where upstream has no fix, ignore exactly one advisory by id (OWASP A06, CWE-1104)
- Gate 120 — Rate limit on credential endpoints. Fix: Attach a limiter middleware to every route that verifies or issues a credential (`express-rate-limit`, `slowapi`, the framework's throttle class) with a shared counter store, since per-process stores undercount behind multiple workers. This gate is presence, not threshold: the number is in **Out of scope**. (OWASP A04, CWE-657)
- Gate 121 — Per-account failure backoff. Fix: Store `failedAttempts` and `lockedUntil` on the user row, increment on the failure branch, (OWASP A04, CWE-657)
- Gate 123 — Result-set ceiling. Fix: Clamp the caller's value to a maximum written literally in the source, and clamp the bottom end too (`|| 20` accepts `-5`, a backwards `take` in Prisma). Always give the query a `take` even when the caller asked for nothing, and keep the maximum a literal so raising it is a reviewed diff. (OWASP A04, CWE-657)
- Gate 125 — Request-sized work. Fix: Clamp each request-supplied magnitude to a source-literal maximum where it enters the operation, not where it is parsed (a validated copy nothing reads is Gate 43). Where the cost scales on a range or filter, bound the unit — a maximum span, a maximum row count (Gate 123) — or move the work to a queued job with a ceiling instead of running it inline. (OWASP A04, CWE-657)
- Gate 127 — Bounded expansion. Fix: Open each entry and read it through a fixed-size chunk loop, keeping a running total of (OWASP A04, CWE-657)
- Gate 128 — Bounded quantity. Fix: Give the field an inclusive range — `.int().min(1).max(N)` — and bound the array length in the same schema. `N` is a product decision Airtight can't know; the gate only requires that some literal `N` exists. Even a generous cap stops the negative and the billion. (OWASP A04, CWE-657)
